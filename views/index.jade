extends layout

block content
  h1 Train Schedule!!!
  p Select your train below to see its current schedule time.
  #wrapper
    #trainList
      h2 Trains
      #trains.d-flex.flex-row
    #trainInformation
      strong  <span id='trainName'></span>
      #trainSchedule
        strong Select a train to view its current schedule.
